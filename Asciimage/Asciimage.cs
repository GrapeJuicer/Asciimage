using Cv = OpenCvSharp;

namespace Asciimage
{
    public static class Asciimage
    {
        public static AsciiMat Generate(Cv.Mat mat, AsciimageConfig config)
        {
            

            // 仮の戻り値
            return new AsciiMat(new char[0, 0]);
        }
    }
}
